# Canonical AWS Terraform

This directory is the canonical Terraform stack for the migrated production
environment in AWS account `589362963105`, region `ap-southeast-1`.

Run Terraform from this directory only. The root `terraform/` directory is a
legacy pre-migration stack and must not be applied to production.
